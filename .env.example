# ProEdge API gateway — environment
# Copy to .env (gitignored) and fill in as needed.

# Unlocks screenshot vision extraction (/extract) and the AI analyst (/explain).
# Everything else runs without it.
ANTHROPIC_API_KEY=

# API gateway port (uvicorn)
PROEDGE_API_PORT=8000
