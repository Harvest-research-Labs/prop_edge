"""ProEdge API gateway — thin FastAPI wrappers around the existing models.

Phase 2, Step 1: establish stable contracts around current behavior. No model
logic is changed here; each service delegates to model/ and sources/.
"""
