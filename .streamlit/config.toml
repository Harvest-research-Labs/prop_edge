[theme]
base = "dark"
primaryColor = "#8B5CF6"
backgroundColor = "#0A0F1E"
secondaryBackgroundColor = "#111A2E"
textColor = "#E7EBF5"
font = "sans serif"

[server]
# PropEdge's own port, so it never collides with ComplyForge on 8501
port = 8502

[browser]
gatherUsageStats = false
